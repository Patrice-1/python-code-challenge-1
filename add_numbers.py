#Write add number function that takes in two parameters, num1 and num2.
#The numbers could be of type integer or float

def add_numbers(num1, num2):

    #add the two numbers and return the result
    return num1 + num2

#test the function with two numbers
print(add_numbers(5, 10))